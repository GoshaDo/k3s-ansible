web:
  ingress:
    enabled: true
    annotations: {}
    kubernetes.io/ingress.class: traefik
    traefik.frontend.rule.type: PathPrefix
    hosts:
      - temporal.local
    tls:
     - secretName: temporal-tls
       hosts:
         - temporal.local
grafna:
  enabled: false
prometheus:
  enabled: false